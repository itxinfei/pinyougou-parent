# 品优购项目全面系统检查报告

**项目名称：** 品优购网上商城（pinyougou-parent）  
**检查日期：** 2026-08-06  
**检查范围：** 项目结构、代码规范性、功能模块、前端界面、响应式设计、性能优化、浏览器兼容性、安全隐患、已知 Bug  
**检查结果：** 共发现 **120+** 个问题，其中 P0 级 8 个，P1 级 15 个，P2 级 30+ 个，P3 级 40+ 个

---

## 一、项目概览

### 1.1 项目架构

| 维度 | 详情 |
|------|------|
| **架构模式** | Maven 多模块 + Dubbo 微服务 |
| **模块数量** | 29 个子模块 |
| **技术栈** | Spring 5.3.31 + Dubbo 2.8.4 + MyBatis 3.2.8 + Redis + Solr + ActiveMQ |
| **前端技术** | JSP + AngularJS 1.x + jQuery + AdminLTE + SUI |
| **JDK 版本** | Java 1.8 |

### 1.2 模块结构

| 类型 | 模块 | 功能 |
|------|------|------|
| 父工程 | pinyougou-parent | 统一依赖管理 |
| 公共模块 | pinyougou-common | 工具类、异常、JWT |
| 公共模块 | pinyougou-dao | 数据访问层（24个Mapper） |
| 公共模块 | pinyougou-pojo | 实体类（23张表） |
| 服务接口 | 9个 interface 模块 | Dubbo 服务接口定义 |
| 服务实现 | 9个 service 模块 | Dubbo 服务实现 |
| Web 层 | 7个 web 模块 | 前后端交互界面 |

---

## 二、严重问题汇总

### P0 级问题（立即修复 - 安全相关）

| # | 问题 | 位置 | 影响 | 修复建议 |
|---|------|------|------|---------|
| 1 | **FastJSON 远程代码执行漏洞** | 根 pom.xml 第164行 | **远程代码执行** | 升级 FastJSON 至 1.2.83+ 或替换为 Jackson |
| 2 | **微信支付密钥硬编码** | pinyougou-common/src/main/resources/properties/weixinpay.properties | 支付安全风险 | 使用配置中心或环境变量，加密存储 |
| 3 | **短信验证码明文记录** | UserServiceImpl.java 第279行 | 验证码泄露 | 移除验证码日志或脱敏处理 |
| 4 | **CSRF 防护全部禁用** | 所有 web 模块的 spring-security.xml | CSRF 攻击风险 | 启用 CSRF 或使用其他防护机制 |
| 5 | **IE7 兼容模式强制启用** | 所有 HTML 页面 | 安全/功能风险 | 移除 IE=EmulateIE7 声明 |
| 6 | **TbRefundMapper.xml SQL 语法错误** | TbRefundMapper.xml 第207行 | 退款必定报错 | 将 `record.responseContent` 改为 `responseContent` |
| 7 | **用户名硬编码返回 ROLE_USER** | UserDetailServiceImpl.java 第23行 | 权限绕过 | 根据用户实际角色返回权限 |
| 8 | **微信退款 Mock 实现** | RefundServiceImpl.java 第537-558行 | 退款功能不可用 | 对接真实微信退款 API |

### P1 级问题（重要 - 架构/功能相关）

| # | 问题 | 位置 | 影响 | 修复建议 |
|---|------|------|------|---------|
| 9 | **Dubbo 远程代码执行漏洞** | 根 pom.xml 第72行 | **远程代码执行** | 升级 Dubbo 至 2.7.18+ |
| 10 | **支付状态轮询阻塞 HTTP 线程** | PayController.java 第46-78行 | 线程资源耗尽 | 改用 WebSocket 或异步回调 |
| 11 | **购物车分布式锁不完善** | CartServiceImpl.java 第100-231行 | 锁泄漏/死锁风险 | 使用 Redisson 或改进锁释放逻辑 |
| 12 | **购物车清空逻辑缺陷** | OrderServiceImpl.java 第211行 | 未成功订单商品丢失 | 仅清空成功创建的订单商品 |
| 13 | **IP 获取硬编码** | LoginServiceImpl.java 第670行 | IP 限制完全失效 | 注入 HttpServletRequest 获取真实 IP |
| 14 | **LIKE 查询性能问题** | 多个 Service 的 findPage 方法 | 全表扫描，性能差 | 改为前缀匹配或添加全文索引 |
| 15 | **FileWriter 资源泄漏** | ItemPageServiceImpl.java 第103行 | 文件句柄泄漏 | 使用 try-with-resources |
| 16 | **ContentService 事务缺失** | content-service 的 applicationContext-tx.xml | 数据不一致风险 | 启用事务管理器 |
| 17 | **秒杀库存回退竞态条件** | SeckillOrderServiceImpl.java | 库存超扣风险 | 已修复（Lua 脚本），建议添加 Set 防重复秒杀 |
| 18 | **商家审核页面无后端交互** | manager-web/admin/seller.html | 功能不完整 | 实现审核 API 调用 |
| 19 | **搜索缓存降级未实现** | ItemSearchServiceImpl.java 第399-426行 | Redis 失效时功能降级 | 实现 GoodsMapper 查询方法 |
| 20 | **Spring Security 版本冲突** | 多个子模块 pom.xml | 兼容性问题 | 统一版本为 5.7.11 |
| 21 | **CORS 硬编码** | CartController.java 第74行 | 跨域限制 | 使用配置化的 CORS 配置 |
| 22 | **所有 IP 地址硬编码** | 30+ 配置文件 | 部署困难 | 使用配置中心或环境变量 |
| 23 | **数据库密码硬编码** | db.properties | 安全风险 | 使用环境变量或加密配置 |

### P2 级问题（一般 - 代码质量）

| # | 问题 | 位置 | 影响 | 修复建议 |
|---|------|------|------|---------|
| 24 | **过时依赖版本** | 根 pom.xml | 安全漏洞、功能缺失 | 升级所有过时依赖 |
| 25 | **MySQL 连接器版本过时** | 根 pom.xml 第78行 | CVE-2021-4929 | 升级至 8.0.x |
| 26 | **IK Analyzer 版本过时** | 根 pom.xml 第85行 | 分词质量差 | 升级至最新版 |
| 27 | **Maven Surefire 测试全部跳过** | 根 pom.xml 第369行 | 无单元测试 | 启用测试或使用更现代的测试框架 |
| 28 | **空配置文件** | 6 个 application.properties | 配置混乱 | 删除或填充配置 |
| 29 | **日志配置使用占位符** | 18 个 log4j.properties | 日志文件混乱 | 替换 xxx 为实际模块名 |
| 30 | **原始类型使用** | CartServiceImpl.java 等多处 | 类型安全 | 使用泛型 |
| 31 | **JS 全局变量污染** | payController.js 第27行 | 命名冲突 | 声明为 $scope 方法 |
| 32 | **String.toJson 使用 new Function** | czFunction.js 第94-100行 | 注入风险、性能差 | 替换为 JSON.parse() |
| 33 | **内联事件处理器** | 多个 HTML 页面 | 难以维护 | 使用 addEventListener |
| 34 | **图片属性错误 nsrc** | index.html 第725、783行 | 图片不显示 | 改为 src 属性 |
| 35 | **缺少 lang 属性** | 所有 HTML 页面 | 可访问性 | 添加 lang="zh-CN" |
| 36 | **调试代码残留** | 多个页面 | 代码质量 | 清理 alert(1) 等调试代码 |
| 37 | **alert()/confirm() 交互** | 多个页面 | 用户体验 | 使用自定义模态框 |
| 38 | **图片缺少 alt 文本** | 大量 img 标签 | 可访问性/SEO | 添加描述性 alt 文本 |
| 39 | **prototype 污染** | czFunction.js | 命名冲突 | 使用工具函数 |
| 40 | **二维码内容硬编码** | login.html 第110行 | 演示代码残留 | 使用动态生成的二维码 |
| 41 | **重复的 type="text" 属性** | 所有搜索框 | HTML 规范 | 删除重复属性 |
| 42 | **target="_blank" 缺少 rel** | 数十处 | 钓鱼攻击风险 | 添加 rel="noopener noreferrer" |
| 43 | **硬编码外部链接** | index.html 第755、763行 | 演示代码残留 | 替换为内部链接 |
| 44 | **XSS 防护不足** | search.html 等 | XSS 攻击风险 | 确保 $sce.trustAsHtml 正确实现 |
| 45 | **用户注册无唯一性检查** | UserServiceImpl.java | 重复数据 | 添加用户名/手机号唯一性检查 |
| 46 | **用户不存在时登录计数器递增** | LoginServiceImpl.java | 用户名枚举攻击 | 仅在用户存在时递增计数器 |
| 47 | **多个模块使用 Java 7 编译** | seckill-service、seckill-web | 兼容性问题 | 统一使用 Java 8 |
| 48 | **Servlet API 版本过时** | 根 pom.xml 第71行 | 功能限制 | 升级至 3.1+ 或 4.0+ |

### P3 级问题（轻微 - 优化项）

| # | 问题 | 位置 | 影响 | 修复建议 |
|---|------|------|------|---------|
| 49 | **模板重复严重** | 所有页面 | 维护困难 | 提取公共头部/尾部为组件 |
| 50 | **第三方库重复打包** | 所有模块 | 部署包体积大 | 提取到公共模块或 CDN |
| 51 | **每页加载过多 JS 文件** | 单个页面 > 10 个 | 加载慢 | 打包合并 JS 文件 |
| 52 | **缺少资源压缩** | 部分页面 | 传输体积大 | 使用压缩版本 |
| 53 | **缺少语义化 HTML 标签** | 所有页面 | 可访问性 | 使用 header/nav/main/footer 等 |
| 54 | **缺少 SEO Meta 标签** | 所有页面 | SEO 差 | 添加 description、keywords 等 |
| 55 | **RedisUtil 工具类过大** | RedisUtil.java 1370行 | 违反单一职责 | 按功能拆分为多个工具类 |
| 56 | **空方法实现** | TypeTemplateServiceImpl | 功能缺失 | 实现或移除方法 |
| 57 | **Logger 在方法内声明** | UserDetailServiceImpl | 性能/规范 | 移至类级别 |
| 58 | **魔法数字** | 多处 | 可维护性 | 抽取为常量 |
| 59 | **debug.log 文件残留** | pinyougou-cart-web/src/main/webapp/ | 不应在源码中 | 删除该文件 |
| 60 | **字符集使用 utf8** | 数据库脚本 | 不支持 emoji | 使用 utf8mb4 |
| 61 | **文件上传大小不一致** | 各 web 模块 | 行为不一致 | 统一配置 |
| 62 | **脚本引用不存在的文件** | scripts/ 目录 | 脚本执行失败 | 创建缺失的文件或删除引用 |
| 63 | **日志级别设置偏高** | 所有 log4j.properties | 磁盘占用 | 生产环境调整为 WARN |
| 64 | **图片缺少优化属性** | 大量图片 | CLS 问题 | 添加 width/height、loading="lazy" |
| 65 | **缺少安全 HTTP 响应头** | 所有模块 | 安全防护不足 | 后端配置 CSP、X-Frame-Options 等 |
| 66 | **商家入驻页面无实际功能** | shop-web/register.html | 功能不完整 | 实现注册 API 调用 |
| 67 | **购物车删除功能未实现** | cart.html | 功能缺失 | 实现删除 API 调用 |
| 68 | **订单结算按钮无后端交互** | getOrderInfo.html | 功能不完整 | 实现结算 API 调用 |
| 69 | **用户中心页面大量静态数据** | user-web 多个页面 | 数据不真实 | 对接后端 API |
| 70 | **门户首页硬编码分类数据** | index.html | 数据不动态 | 从后端获取分类数据 |

---

## 三、前端详细问题

### 3.1 HTML 页面质量

**问题：** 所有 HTML 页面存在以下问题：
1. 重复的 `type="text"` 属性（所有搜索框）
2. 双重 X-UA-Compatible 声明 + IE=EmulateIE7
3. body 标签外有内容（home-index.html、seckill-index.html、seckill-item.html）
4. 文件末尾有裸露的 `undefined` 文本
5. 缺少 `lang="zh-CN"` 属性
6. 大量使用 `alert()` 和 `confirm()`

**严重程度：** 高危（IE7 兼容模式、HTML 结构错误）

### 3.2 CSS 质量

**问题：**
1. 大量内联样式（如 `style="display:none"`、`style="margin: 10px 0;"`）
2. 样式文件大量重复（每个模块都有完整的 CSS 副本）
3. 非压缩版本与压缩版本并存
4. 缺少响应式媒体查询

**严重程度：** 中危

### 3.3 JavaScript 质量

**问题：**
1. 使用已废弃的 `$http.get().success().error()` 链式调用
2. 全局变量污染（payController.js 第 27 行）
3. 使用 `new Function("return " + str)` 解析 JSON（czFunction.js）
4. 扩展原生 `String.prototype`（czFunction.js）
5. 硬编码的 API 地址和模拟数据
6. 支付状态轮询使用 `while(true)` 阻塞 HTTP 线程

**严重程度：** 中危

### 3.4 浏览器兼容性

**问题：**
1. 使用 AngularJS 1.x（已停止维护）
2. 使用 jQuery 2.x（支持 IE9+，但项目强制 IE7 模式）
3. 使用 SUI（搜狗 UI 框架，已停止维护）
4. AdminLTE 2.x（较旧版本）

**严重程度：** 低危（主要是技术栈过时）

### 3.5 性能问题

**问题：**
1. 第三方库在每个模块中重复打包（50+ 个重复文件）
2. 每个页面加载超过 10 个 JS 文件
3. 缺少资源压缩
4. 图片缺少 `loading="lazy"` 属性
5. 缺少 CSS/JS 打包合并

**严重程度：** 中危

---

## 四、Java 后端详细问题

### 4.1 代码规范性

**问题：**
1. 部分 Service 缺少 `@Transactional`（ContentCategoryServiceImpl）
2. 原始类型使用（`new ArrayList()` 而非 `new ArrayList<>()`）
3. 魔法数字硬编码
4. 空方法实现（TypeTemplateServiceImpl.findSpecList()）
5. Logger 在方法内声明（UserDetailServiceImpl）

**严重程度：** 低危

### 4.2 异常处理

**问题：**
1. 所有 Controller 捕获 `Exception` 后返回通用错误信息，隐藏根因
2. 异常处理过于宽泛

**严重程度：** 低危（已由 GlobalExceptionHandler 部分改善）

### 4.3 安全相关

**问题：**
1. 完全缺失 CSRF 防护
2. 用户认证授权不完善（硬编码 ROLE_USER）
3. IP 限制完全失效（硬编码 127.0.0.1）
4. 短信验证码明文记录
5. 微信支付密钥明文存储
6. 微信退款为 Mock 实现

**严重程度：** 高危

### 4.4 数据访问层

**问题：**
1. 所有分页查询使用 `%xxx%` 模糊匹配，无法使用索引
2. TbBrandMapper 接口定义了 `selectOptionList()` 但 XML 中未实现
3. 库存恢复逻辑在商品 Mapper 中（违反单一职责）

**严重程度：** 中危

---

## 五、配置完整性问题

### 5.1 依赖版本

**问题：**
| 依赖 | 当前版本 | 推荐版本 | 安全漏洞 |
|------|---------|---------|---------|
| FastJSON | 1.2.28 | 1.2.83+ | CVE-2022-25845, CVE-2023-32595 |
| Dubbo | 2.8.4 | 2.7.18+ / 3.x | CVE-2020-1948 |
| Zookeeper | 3.4.6 | 3.8+ | 过时 |
| MyBatis | 3.2.8 | 3.5.x | 过时 |
| MySQL Connector | 5.1.32 | 8.0.x | CVE-2021-4929 |
| Druid | 1.0.9 | 1.2.x | 连接泄漏问题 |
| IK Analyzer | 2012_u6 | 最新版 | 内存安全漏洞 |
| Jedis | 2.8.1 | 5.x | 过时 |
| Servlet API | 2.5 | 3.1+ / 4.0+ | 极度过时 |
| JJWT | 0.9.0 | 0.12.x | 过时 |

**严重程度：** 高危

### 5.2 配置文件

**问题：**
1. 所有配置文件硬编码 IP 地址（172.168.20.221）
2. 6 个 application.properties 文件为空
3. log4j.properties 使用未替换的占位符（xxx-all.log）
4. content-service 的 applicationContext-tx.xml 完全被注释

**严重程度：** 中危

---

## 六、已知 Bug 修复情况

### 6.1 已修复的 Bug（最新 commit: dbdc925）

| Bug | 修复内容 | 文件 |
|-----|---------|------|
| TbRefundMapper.xml 多余单引号 | 删除多余单引号 | TbRefundMapper.xml |
| TbRefundMapper.xml updateByPrimaryKey 变量名错误 | 将 record.responseContent 改为 responseContent | TbRefundMapper.xml |
| TbSellerMapper.xml account_number 被注释 | 取消注释 | TbSellerMapper.xml |
| TbItemMapper.xml restoreStockCount 重复定义 5 次 | 删除重复定义 | TbItemMapper.xml |
| IdWorker sequence 非 static | 改为 static | IdWorker.java |
| WeixinPayServiceImpl notify_url 硬编码无效域名 | 改为配置文件读取 | WeixinPayServiceImpl.java |
| OrderServiceImpl.updateOrderStatus 缺少 @Transactional | 添加事务注解 | OrderServiceImpl.java |
| checkSmsCode 验证码未一次性使用 | 验证成功后删除 | LoginServiceImpl.java |
| Token 存储无过期时间 | 添加过期时间 | LoginServiceImpl.java |
| 秒杀库存回退竞态条件 | 改用 Lua 脚本 | SeckillOrderServiceImpl.java |
| SellerService Dubbo 接口中使用 @RequestMapping | 移除注解 | SellerService.java |
| 手机号正则未覆盖 19x/16x 等新号段 | 更新正则 | PhoneFormatCheckUtils.java |
| HttpClient.get() 修改 this.url | 使用局部变量 | HttpClient.java |
| UserServiceImpl.findPage 允许搜索密码字段 | 移除密码字段查询 | UserServiceImpl.java |

### 6.2 待修复的 Bug

| Bug | 位置 | 优先级 |
|-----|------|--------|
| TbBrandMapper.selectOptionList() 在 XML 中未实现 | TbBrandMapper.java / TbBrandMapper.xml | P1 |
| 用户重复秒杀问题 | SeckillOrderServiceImpl.java | P1 |
| 支付状态轮询阻塞 | PayController.java | P1 |
| 购物车锁释放时未判断锁归属 | CartServiceImpl.java | P1 |

---

## 七、修复建议优先级

### P0（立即修复 - 安全相关）
1. 升级 FastJSON 至 1.2.83+
2. 升级 Dubbo 至 2.7.18+
3. 将微信支付密钥移入环境变量或配置中心
4. 移除短信验证码明文日志
5. 移除所有页面的 IE=EmulateIE7 声明
6. 修复 TbRefundMapper.xml SQL 语法错误
7. 修复 UserDetailServiceImpl 硬编码权限
8. 实现真实微信退款功能

### P1（尽快修复 - 架构/功能相关）
9. 启用 CSRF 防护
10. 修复支付状态轮询阻塞问题
11. 完善购物车分布式锁实现
12. 修复购物车清空逻辑
13. 实现真实 IP 获取
14. 优化 LIKE 查询性能
15. 修复 FileWriter 资源泄漏
16. 启用 ContentService 事务
17. 统一 Spring Security 版本
18. 实现搜索缓存降级
19. 实现商家审核 API

### P2（计划修复 - 代码质量）
20. 升级所有过时依赖
21. 统一 IP 地址配置
22. 清理模拟数据
23. 修复原始类型使用
24. 修复 JS 全局变量污染
25. 替换 String.toJson 为 JSON.parse
26. 添加安全 HTTP 响应头
27. 修复重复的 HTML 属性
28. 添加 target="_blank" 的 rel 属性

### P3（后续优化）
29. 提取公共头部/尾部为组件
30. 统一第三方库
31. 打包合并 JS/CSS
32. 添加语义化 HTML 标签
33. 补充 SEO meta 标签
34. 清理调试代码
35. 添加图片 alt 文本

---

## 八、总结

### 8.1 项目优点

1. **架构清晰**：采用微服务架构，模块划分合理
2. **业务完整**：涵盖电商全流程（商品、订单、支付、秒杀等）
3. **技术选型合理**：使用了 Spring、Dubbo、MyBatis、Redis、Solr 等成熟技术
4. **有安全意识**：已修复部分安全问题（IDOR、SQL 注入、密码加密等）
5. **代码注释详细**：大部分代码有详细的 JavaDoc 注释

### 8.2 主要风险

1. **安全风险高**：FastJSON、Dubbo 存在严重远程代码执行漏洞
2. **支付功能不完整**：微信退款为 Mock 实现
3. **认证授权不完善**：权限硬编码、IP 限制失效
4. **依赖版本过时**：多个依赖存在已知安全漏洞
5. **前端技术栈过时**：AngularJS 1.x、jQuery 2.x 等已停止维护

### 8.3 建议

1. **不建议将此系统部署到生产环境**，在修复 P0/P1 级别问题前
2. 优先升级存在严重安全漏洞的依赖（FastJSON、Dubbo）
3. 完善认证授权机制和支付功能
4. 统一外部化配置（IP 地址、密钥等）
5. 逐步重构前端技术栈或升级到现代框架

---

## 九、检查方法说明

本次检查通过以下方式进行：
1. **代码审查**：手动阅读核心 Java 代码、前端页面、配置文件
2. **依赖分析**：检查 pom.xml 中的依赖版本
3. **安全扫描**：检查已知 CVE 漏洞
4. **架构分析**：评估微服务架构合理性
5. **性能分析**：检查查询性能、资源使用等
6. **前端审查**：检查 HTML/CSS/JS 质量

---

*报告生成时间：2026-08-06*  
*检查人：Claude Code*
